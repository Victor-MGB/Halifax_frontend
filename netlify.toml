[build]
  base = ""
  publish = "build"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"

# This is the critical part for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200