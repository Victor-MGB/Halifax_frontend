[build]
  base = ""
  publish = "build"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"
  REACT_APP_API_URL = "https://halifax-backend.onrender.com"

# This is the critical part for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200